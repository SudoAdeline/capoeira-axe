# Session — 8 March 2026

## What we did

### 1. Full UI redesign — dark to light theme
Rewrote every component and page from the dark `#1A0F08` palette to a warm light theme (`#FFFBF5` background, `#2D1B0E` text, `#E8652B` accent, `#D4A843` gold). Files touched:
- `src/index.css`
- `src/components/Layout.jsx`, `CalendarView.jsx`, `EventCard.jsx`, `LoginModal.jsx`
- `src/pages/Home.jsx`, `EventDetail.jsx`, `SubmitEvent.jsx`, `MyEvents.jsx`, `AdminDashboard.jsx`, `Privacy.jsx`

### 2. Splash intro
Added an animated landing screen to `Home.jsx`:
- Big "AXÉ" gradient text (DM Sans, weight 500)
- Capoeira quote: *"Capoeira é pra homem, menino e mulher — só não aprende quem não quer."*
- "ENTER THE RODA" button with phased animations
- Greeting "Salve, {name}!" for logged-in users
- Only shows once per browser session (uses `sessionStorage`)

### 3. Flowing animated background
Moved to `Layout.jsx` so it appears on all pages:
- 3 animated SVG sine-wave paths (gold + orange)
- 5 floating circle elements
- All at low opacity behind content

### 4. Event cards upgrade
- Color-coded left border per event type
- Type + free badges
- Removed scope badges (scope column removed from submit form too)

### 5. RSVP attendee list with capoeira group names
- Added `capoeira_group` column to `profiles` table
- New SQL migration: `supabase/migrations/002_add_group.sql`
- Sign-up form now has optional "Capoeira Group" field
- User menu dropdown has editable group field for existing users
- Event detail page shows full attendee list (name + group) — **visible only to the event creator**
- Files: `AuthContext.jsx`, `LoginModal.jsx`, `Layout.jsx`, `EventDetail.jsx`
- Translation keys added to all 4 locales (EN/PT/DE/FR)

### 6. Improved Google Maps links
- Maps queries now combine venue + address + city + country for better results
- Shows a maps link even when only city is provided (no venue/address)

### 7. Commits pushed
```
70c991f Improve maps links, skip repeat splash, clean up scope
11d6b97 Add RSVP attendee list with capoeira group names
d93f6ce Redesign UI — light theme, splash intro, animated background
```

## Current state

- **Live site:** capoeira-axe.vercel.app (Vercel auto-deploys from GitHub)
- **Supabase:** yfhnuarnordnwgxqkpmq.supabase.co — auth working, RLS policies active
- **Env vars:** set in both `.env` (local) and Vercel dashboard
- **Admin:** user "Alegria" is admin
- **Google OAuth:** not yet configured (needs Google Cloud Console setup — it's free)

## Still to do

- [ ] Set up Google OAuth in Google Cloud Console + Supabase provider settings
- [ ] Add Vercel URL (`https://capoeira-axe.vercel.app`) to Supabase redirect URLs if not done
- [ ] Consider adding image upload for events
- [ ] Ship it to the capoeira community
